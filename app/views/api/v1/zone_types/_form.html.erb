<%= form_with(model: api_v1_zone_type) do |form| %>
  <% if api_v1_zone_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_v1_zone_type.errors.count, "error") %> prohibited this api_v1_zone_type from being saved:</h2>

      <ul>
        <% api_v1_zone_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
