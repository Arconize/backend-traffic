<%= form_with(model: api_v1_vehicle) do |form| %>
  <% if api_v1_vehicle.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_v1_vehicle.errors.count, "error") %> prohibited this api_v1_vehicle from being saved:</h2>

      <ul>
        <% api_v1_vehicle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
