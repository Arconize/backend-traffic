<%= form_with(model: api_v1_camera) do |form| %>
  <% if api_v1_camera.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(api_v1_camera.errors.count, "error") %> prohibited this api_v1_camera from being saved:</h2>

      <ul>
        <% api_v1_camera.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
